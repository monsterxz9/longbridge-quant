[project]
name = "longbridge-quant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "longbridge>=4.0.5",
    "pandas>=3.0.2",
    "pyarrow>=17.0.0",
]
