# longbridge-quant 环境变量模板
#
# 使用方式二选一:
#   1) 直接在 shell 中 export
#   2) 复制为 .env 后填入值，并用 direnv / dotenv-cli 等工具加载
#
# 本项目不依赖 python-dotenv，加载这个文件不会自动生效。

# Longbridge CLI 二进制路径（默认从 PATH 查找 longbridge）
# LONGBRIDGE_CLI=longbridge

# 本地缓存目录（默认 .cache/）
# LB_CACHE_DIR=.cache

# K线 / 市值缓存过期时间（小时，默认 12）
# LB_CACHE_MAX_AGE_HOURS=12
